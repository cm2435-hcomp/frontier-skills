[project]
name = "frontier-skills"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "python-frontmatter>=1.1.0",
  "pyyaml>=6.0.2",
]

[dependency-groups]
dev = ["pytest>=8.3.5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
